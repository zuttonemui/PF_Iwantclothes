<% rooms.each do |room| %>
  <div>
    <%= link_to dm_user_path(room.chat_partner(current_user)) do %>
      <div>
        <h4>
          <%= opponent_user(room) %>
        </h4>
        <p>
          <%= most_new_message_preview(room) %>
        </p>
      </div>
    <% end %>
  </div>
<% end %>