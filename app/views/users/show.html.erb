<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <%= render "info", user: @user %>
    </div>
    <div class="col-md-1 mypage-menu">
      <% if @user == current_user %>
        <%= render 'mypage_menu', user: @user %>
      <% end %>
    </div>
    <div class="col-md-8">
      <h4>ウォント</h4>
      <%= render 'want_items/index', want_items: @want_items %>

      <h4>リコメンド</h4>
      <div class="row d-flex">
        <%= render 'recommends/index', recommends: @recommends %>
      </div>
    </div>
  </div>
</div>
